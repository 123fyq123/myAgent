# 后端接口验证记录

验证时间：2026-07-19

## 已修复

- [x] 补齐 `skills`、`github_sources`、`agent_skills` 表，修复 Skill 列表、创建、来源管理、Agent 绑定 Skill 的数据库错误。
- [x] 启动时自动执行幂等 schema 补丁，补齐 `agents.agent_mode`、`agents.deep_config` 以及 Skills 相关表，避免旧 SQL 导入后再次缺表/缺字段。
- [x] 修复 RAG 未配置 Embedding 模型时的 panic：现在返回 `EmbeddingConfig不存在`，后台文档处理会把文档置为 `failed`，不会打崩 app。
- [x] 修复 Elasticsearch 容器持续重启问题：实际挂载的 `elasticsearch.yml` 被错误保存成二进制文档，已备份原文件并替换为 UTF-8 YAML 配置。
- [x] 修复本地 Skill 删除后同名无法重新创建的问题：现在遇到同名软删除记录会恢复并更新为当前本地 Skill 配置。
- [x] 修复工作流 `textCombine` 节点对配置结构过度强转导致的 panic：现在兼容字符串和 `{fieldValue: ...}` 两种格式。
- [x] 修复未配置 SMTP 时注册失败的问题：当前邮箱服务未配置时，注册会自动跳过邮箱验证并创建可登录用户。
- [x] 修复未配置 SMTP 时找回密码错误不清晰的问题：现在返回 `邮箱服务未配置`。
- [x] 修复 core/a2a-server/mcp-server 中旧的高德 `ApiKey/GdApiKey` 引用：改为 `AMAP_API_KEY` 环境变量，未配置时跳过天气工具注册。

## 已验证通过

- [x] `GET /health` 返回 `code=200`。
- [x] `POST /api/v1/auth/login` 使用 `admin / 123456` 登录成功。
- [x] `POST /api/v1/auth/refresh-token` 刷新 token 成功。
- [x] `POST /api/v1/auth/register` 在未配置 SMTP 时可以注册基础账号，并自动跳过邮箱验证。
- [x] 未配置 SMTP 时，新注册用户可直接登录。
- [x] `POST /api/v1/auth/forgot-password` 在未配置 SMTP 时返回业务错误 `邮箱服务未配置`，app 保持可用。
- [x] `POST /api/v1/skills/list` 可正常返回。
- [x] `POST /api/v1/skills` 可创建本地 Skill。
- [x] `POST /api/v1/skills` 使用真实存在的 `baseDir/name/SKILL.md` 可完成创建、列表查询、删除闭环。
- [x] 删除本地 Skill 后，可以使用同名 `baseDir/name/SKILL.md` 再次创建。
- [x] `GET /api/v1/skills/all` 可返回全部 Skill。
- [x] `PUT /api/v1/skills` 可更新 Skill。
- [x] `DELETE /api/v1/skills/:id` 可删除 Skill。
- [x] `POST /api/v1/skills/sources` 可创建 GitHub Source。
- [x] `POST /api/v1/skills/sources/list` 可返回 Source 列表。
- [x] `GET /api/v1/skills/sources/:id` 可返回 Source 详情。
- [x] `PUT /api/v1/skills/sources` 可更新 Source。
- [x] `DELETE /api/v1/skills/sources/:id` 可删除 Source。
- [x] `POST /api/v1/agents/create` 可创建 Agent。
- [x] `POST /api/v1/agents/list` 可返回 Agent 列表。
- [x] `POST /api/v1/agents/:id/skills` 可绑定 Skill。
- [x] `POST /api/v1/agents/:id/skills/:skillId` 可解绑 Skill。
- [x] `POST /api/v1/agents/sessions` 可创建会话。
- [x] `GET /api/v1/agents/sessions?agentId=...` 可返回会话列表。
- [x] `DELETE /api/v1/agents/sessions/:sessionId` 可删除会话。
- [x] `DELETE /api/v1/agents/:id` 可删除 Agent。
- [x] `POST /api/v1/knowledge/` 可创建知识库。
- [x] `POST /api/v1/knowledge/list` 可返回知识库列表。
- [x] `GET /api/v1/knowledge/:id` 可返回知识库详情。
- [x] `PUT /api/v1/knowledge/:id` 可更新知识库。
- [x] `POST /api/v1/agents/:id/knowledge-bases` 可绑定知识库。
- [x] `DELETE /api/v1/agents/:id/knowledge-bases/:kbId` 可解绑知识库。
- [x] `GET /api/v1/knowledge/:id/documents` 可返回文档列表。
- [x] `POST /api/v1/knowledge/:id/documents` 可上传文档并创建文档记录。
- [x] 未配置 Embedding 模型时，文档后台处理失败但 app 保持可用。
- [x] 未配置 Embedding 模型时，`POST /api/v1/knowledge/:id/search` 返回业务错误 `EmbeddingConfig不存在`，app 保持可用。
- [x] Elasticsearch 已恢复可用，`GET /_cluster/health` 返回 `status=green`。
- [x] `DELETE /api/v1/knowledge/:id` 可删除知识库。
- [x] `POST /api/v1/provider-configs/` 可创建模型 Provider 配置。
- [x] `GET /api/v1/provider-configs/` 可返回 Provider 配置列表。
- [x] `POST /api/v1/llms/` 可创建 LLM 配置。
- [x] `GET /api/v1/llms/?modelType=chat` 可返回指定类型模型列表。
- [x] `GET /api/v1/llms/all?modelType=chat` 可返回全部指定类型模型。
- [x] `POST /api/v1/tools/` 可创建 MCP Tool 配置。
- [x] `GET /api/v1/tools/` 可返回工具列表。
- [x] `PUT /api/v1/tools/:id` 可更新工具。
- [x] `DELETE /api/v1/tools/:id` 可删除工具。
- [x] `GET /api/v1/tools/mcp/:mcpId/tools` 在 MCP 服务不可达时返回业务错误 `获取McpTools失败`，app 保持可用。
- [x] `POST /api/v1/tools/:id/test` 对 MCP Tool 返回明确的 `success=false` 提示，不再误报 `工具不存在`。
- [x] `POST /api/v1/workflows/create` 可创建工作流。
- [x] `POST /api/v1/workflows/list` 可返回工作流列表。
- [x] `GET /api/v1/workflows/:id` 可返回工作流详情。
- [x] `PUT /api/v1/workflows/update` 可更新工作流。
- [x] `POST /api/v1/workflows/save` 可保存工作流图数据。
- [x] `POST /api/v1/workflows/execute` 可执行最小 `start -> textCombine -> end` 工作流。
- [x] `POST /api/v1/workflows/execute` 对 `textCombine` 字符串模板配置不再 panic，返回正常执行结果。
- [x] `DELETE /api/v1/workflows/:id` 可删除工作流。
- [x] `POST /api/v1/nodes/test` 可测试 `textCombine` 节点。
- [x] `POST /api/v1/agents/chat` 在未配置 Chat 模型时返回明确的 `ProviderConfig不存在` 流式错误，app 保持可用。
- [x] `GET /api/v1/subscription/current` 可返回当前用户订阅信息。
- [x] `GET /api/v1/a2a/list` 可返回 Agent 市场列表。
- [x] `POST /api/v1/a2a/getAgentCard` 在远程 Agent URL 不可达时返回业务错误 `获取AgentCard失败`，app 保持可用。
- [x] `POST /api/v1/a2a/saveAgentCard` 在远程 Agent URL 不可达时返回业务错误 `获取AgentCard失败`，app 保持可用。
- [x] `POST /api/v1/auth/logout` 返回成功，占位满足前端退出登录调用。
- [x] `POST /api/v1/agents/all` 可返回当前用户全部 Agent。
- [x] `POST /api/v1/workflows/all` 可返回当前用户全部工作流。
- [x] `GET /api/v1/settings` 返回基础版空配置。
- [x] `GET /api/v1/templates` 返回基础版空模板列表。
- [x] `GET /api/v1/storage/files` 返回基础版空文件列表。
- [x] `GET /api/v1/vector-db/collections` 返回基础版空集合列表。
- [x] `GET /api/v1/relational-db/tables` 返回基础版空数据表列表。
- [x] `GET /api/v1/subscription/plans` 返回基础版 free 订阅计划。

## 构建与测试

- [x] `backend/app`: `go test ./...` 通过。
- [x] `backend/core`: `go test ./...` 通过。
- [x] `backend/common`: `go test ./...` 通过。
- [x] `backend/model`: `go test ./...` 通过。
- [x] `backend/mcp-server`: `go test ./...` 通过。
- [x] `backend/benchmark`: `go test ./...` 通过。
- [x] `backend/a2a-server`: `go build .` 通过。

## 暂时跳过

- [ ] Agent 对话真实模型回复：需要 Chat 模型 Provider、模型名和 API Key。
- [ ] RAG 向量化成功、向量检索成功、生成式回答成功：需要 Embedding 模型配置、Chat 模型配置，以及可用向量服务。
- [ ] GitHub Skill 安装真实仓库：需要网络访问 GitHub。
- [ ] 真实远程 A2A Agent Card 拉取和保存：需要可访问的 A2A 服务地址。
- [ ] 模板创建/导入导出、云存储上传下载、向量库/关系库写入、订阅支付等完整业务：基础版当前返回空数据或 `当前基础版暂未实现该接口`，如要真实可用需要补业务模块。
